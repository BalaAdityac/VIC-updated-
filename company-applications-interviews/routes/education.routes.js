const r=require("express").Router(),a=require("../middlewares/auth"),v=require("../middlewares/validate"),c=require("../controllers/education.controller"),s=require("../validations/education.validation");r.use(a);r.post("/",v(s.createEducationSchema),c.create);r.get("/",c.list);r.put("/:id",v(s.updateEducationSchema),c.update);r.delete("/:id",v(s.idSchema),c.remove);module.exports=r;